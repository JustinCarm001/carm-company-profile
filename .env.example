# Environment Configuration for carm-company-profile

# =============================================================================
# DEVELOPMENT MODE (Local Files)
# =============================================================================
# Default - uses local JSON files in ./profiles directory
# No additional configuration needed!

ENVIRONMENT=development
PROFILES_DIR=./profiles


# =============================================================================
# PRODUCTION MODE (Azure Blob Storage)
# =============================================================================
# Automatically switches to Azure when these are set:

# ENVIRONMENT=production
# AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=youraccountname;AccountKey=youraccountkey;EndpointSuffix=core.windows.net
# AZURE_STORAGE_CONTAINER_NAME=company-profiles
# AZURE_STORAGE_TIMEOUT_SECONDS=30


# =============================================================================
# How to Get Azure Connection String:
# =============================================================================
# 1. Go to Azure Portal (portal.azure.com)
# 2. Navigate to your Storage Account
# 3. Go to "Access keys" under "Security + networking"
# 4. Copy "Connection string" from key1 or key2
# 5. Paste it above as AZURE_STORAGE_CONNECTION_STRING


# =============================================================================
# How Storage Auto-Detection Works:
# =============================================================================
# IF ENVIRONMENT=production AND Azure secrets exist:
#     → Use Azure Blob Storage
# ELSE:
#     → Use Local File Storage
#
# This means:
# - Dev: Just works with local files (no setup)
# - Prod: Add secrets, automatic cloud storage
# - Deploy: Change ENVIRONMENT=production, add Azure secrets, done!
